import { ToastPosition } from 'react-toastify';

export const defaultMessagePlacement = {
    position: 'bottom-right' as ToastPosition,
    autoClose: 3000,
    pauseOnHover: false,
    hideProgressBar: true,
};

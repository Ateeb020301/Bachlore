# .github/workflows/publish.yml
name: Publish 🚀

on:
  workflow_dispatch:
  push:
    branches:
    - main

env:
  AZURE_WEBAPP_NAME: current-time-service
  AZURE_WEBAPP_PACKAGE_PATH: "./publish"

jobs:
  publish:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '15.0.x'
          
      - name: Restore
        run: dotnet restore ./webstep/webstep
        
      - name: Build
        run: dotnet build ./webstep/webstep --configuration Release --no-restore
        
      - name: Publish
        run: dotnet publish ./webstep/webstep --configuration Release --no-build --output '${{ env.AZURE_WEBAPP_PACKAGE_PATH }}'
        
     